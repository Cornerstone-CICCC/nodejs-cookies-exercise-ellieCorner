<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Home</title>
  </head>
  <body>
    <h1>Home</h1>
    <% if (user) { %>
    <p>
      Welcome, <%= user %>! <a href="/logout">Logout</a>
      <a href="/profile">Profile</a>
    </p>
    <% } else { %>
    <p>Please <a href="/login">log in.</a></p>
    <% } %>

    <p>This is for nodejs cookies exercise</p>
  </body>
</html>
